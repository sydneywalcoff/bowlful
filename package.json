{
  "name": "bowlful",
  "version": "1.0.0",
  "description": "A React application to store recipes.",
  "main": "server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd server && npm start",
    "start:dev": "concurrently \"npm start\" && \"cd client && npm run dev\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
